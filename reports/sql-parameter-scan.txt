SQL Parameter Scan Report - ProductService Data

Scanned directory:
- src/Services/ExpressRecipe.ProductService/Data

Files scanned (high level):
- All repository and adapter files under the Data directory, plus key DAL adapters and staging helpers.

Summary:
- No missing SQL parameter declarations were detected in the primary Data files reviewed: IngredientRepository, IngredientRepositoryAdapter, ProductRepository, ProductRepositoryAdapter, ProductImageRepositoryAdapter, ProductStagingRepositoryAdapter, AllergenRepository, and related helpers.
- Dynamic SQL that builds parameterized queries (chunking) uses matching parameter names and then adds SqlParameter arrays via AddRange or the CreateParameter helper.
- INSERT statements that use OUTPUT INSERTED.Id return the inserted Guid and have corresponding parameter declarations.

Notes / Observations (manual review):
- `IngredientRepositoryAdapter.GetIngredientIdsByNamesAsync` and `IngredientRepository.GetIngredientIdsByNamesAsync` build `OR`-joined conditions and supply matching parameters (`@p{i}` / `@Name{i}`) via `SqlParameter` and `CreateParameter` respectively. These look consistent.
- `ProductStagingRepositoryAdapter.InsertStagingProductAsync` uses `OUTPUT INSERTED.Id` and passes SqlParameter[] that match the placeholders in the SQL.
- `ProductImageRepositoryAdapter.AddImageAsync` uses `OUTPUT INSERTED.Id` and casts the scalar to `Guid` — ensure the DB actually returns a Guid (it does via NEWID() in VALUES). This is correct but fragile if schema changes; consider `Guid.Parse` fallback only if null/DB returns string.
- `AllergenRepository` constructs complex WHERE clauses using named parameters `@Allergen{i}` and adds SqlParameter instances. These are consistent.
- The legacy `SqlHelper` `CreateParameter` helper is used extensively; ensure it always prefixes parameter names with `@` (current usage shows `CreateParameter("@Name", value)` so this is consistent).

Recommendations:
- Add a small automated unit test or analyzer that parses Data files for SQL placeholders (`@\w+`) and parameter creation sites to prevent regressions. This can be a Roslyn analyzer or simple test that regex-scans files during CI.
- Validate at runtime in integration tests that key flows (staging insert, product create/update, ingredient bulk create) succeed against a test SQL Server instance; this will catch any subtle mismatches not visible via static scan.
- Consider standardizing parameter creation to always use the `CreateParameter` helper or a small wrapper to avoid subtle naming mismatches when manually adding `SqlParameter` inline.

Conclusion:
- Manual/static scan of the core data-access files in ProductService did not find missing parameter declarations or obvious placeholder mismatches. The codebase appears consistent in its param handling for the SQL strings inspected.

If you want, I can:
- Run a stricter automated scan across every file under the Data directory and report exact placeholder lists vs declared parameter lists per file (produces CSV/JSON report).
- Add a simple unit-test (in a test project) that performs the automated scan during CI.

Report generated by: GitHub Copilot
Timestamp: 